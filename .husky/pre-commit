#!/usr/bin/env sh

# Run Biome on staged files
npx @biomejs/biome check --write --staged --no-errors-on-unmatched

# Note: Tests are disabled in pre-commit hook due to failing tests
# Run tests manually: npm test
